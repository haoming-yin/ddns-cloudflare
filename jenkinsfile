pipeline {
    agent {
        label "pi"
    }

    stages {
        stage("Demo step") {
            steps {
                sh "printenv"
            }
        }
    }
 
    post {
        always {
            cleanWs()
        }
    }
}